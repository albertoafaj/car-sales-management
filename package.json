{
  "name": "carsales-manager",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start-back": "cd ./backend && nodemon --inspect ./src/server.js",
    "start-front": "cd ./frontend && npm start"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
